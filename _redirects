# www -> apex is handled by a Cloudflare Redirect Rule at the zone level.
# Workers static-assets _redirects only allows relative paths (no absolute/
# cross-host URLs), unlike Cloudflare Pages. See DEPLOY.md.